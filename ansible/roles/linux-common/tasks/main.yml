---
- name: Update software cache (Debian)
  ansible.builtin.apt:
    update_cache: true
  become: true
  when: ansible_os_family == "Debian"

- name: Update software cache (RedHat)
  ansible.builtin.dnf:
    update_cache: true
  become: true
  when: ansible_os_family == "RedHat"

- name: Configure terminal
  ansible.builtin.include_role:
    name: linux-terminal

- name: Configure desktop environment
  ansible.builtin.include_role:
    name: linux-ui
